# Polaris M1 验收结论

## 状态：PASS（2026-06 第一周）

M1 阶段已完成。本阶段在 Qwen3-0.6B 上跑通第一组 GSM8K Math SFT，建立了本地 post-training 最小闭环：数据构造、格式协议、baseline eval、SFT 数据生成、10-step sanity、完整 SFT、sample diff 审阅、run_report 记录。

---

## 已完成内容

### 评估协议（D1）
- `scripts/eval_math.py`：支持 answer extraction、format checker、pass/fail 判定。
- 三个核心指标拆分：pass_at_1（答案正确性）、answer_extraction_success（答案可抽取性）、format_adherence（格式遵循率）。
- 手写 toy samples 覆盖 3+ 种格式场景和 3+ 种答案场景。
- 确认 Qwen3 中 `<|im_start|>`、`</think>` 属于对话边界（chat template 负责），`<think>`、`</think>`、`\boxed{}` 是普通文本协议（需 SFT 学出）。

### 数据集选择与治理（D2）
- 比较 GSM8K、OpenR1-Math-220K、MATH 三个数据集的字段结构、答案格式和适配风险。
- 选择 GSM8K 作为 M1 数据源：答案稳定、难度适中、自动判分噪声低。
- `data/math/manifest.yaml`：记录数据源、license、筛选规则、split 协议。
- `data/math/baseline_screening/screening_plan.md`：筛选目标、方法、验收标准。
- 数据构造可复现，不依赖记忆回溯。

### MLX 环境与模型加载（D2.5）
- 验证 `import mlx; import mlx_lm` 不报错。
- `scripts/smoke/load_model.py`：加载 MLX 版 Qwen3-0.6B，生成短文本，记录加载时间和内存占用。
- `docs/feasibility_reports/qwen3_0_6b_load_smoke.md`：环境信息和加载结果。

### Baseline Eval（D3）
- 在 30 条 test 数据上跑 Qwen3-0.6B baseline。
- 确认模型已有一定 GSM8K 能力，但格式遵循不稳定。
- 每个正式实验通过 registry 注册，记录 metrics、hardware snapshot、sample output、run 状态。
- Baseline run 目录完整：config.yaml、run_meta.yaml、metrics/、samples/。

### SFT 数据构造（D4）
- 定义目标输出格式（配置化，不硬编码）：
  ```yaml
  format:
    reasoning_style: think_tags
    final_answer_style: boxed
    target_template: |
      <think>
      {reasoning}
      </think>
      \boxed{{{answer}}}
  ```
- `scripts/prepare_math_data.py`：读取原始数据，构造 messages，按 template 构造 target，过滤异常样本。
- 生成 `data/math/splits/sft_v1.jsonl`（500 条）。

### 10-step SFT Sanity（D4）
- 验证 MLX/LoRA 训练链路：chat template、LoRA target module、loss、checkpoint、eval、日志。
- 发现并解决 chat template 双 think block 冲突问题。
- 发现并修复关键 loss 对齐错误（self-token prediction → next-token prediction）。
- 加入 `debug_batch.json` 文本级调试能力。

### 完整 SFT 实验（D5）
- 最终有效 run：`runs/000030_qwen3_0_6b_sft_gsm8k_500`（status=completed）。
- 训练集 500 条，训练 375 steps，loss 从 1.108333 降到 0.032215，无 NaN/Inf。
- 实现 `runs/.train_sft.lock` 互斥锁，防止并发训练冲突。
- 训练脚本默认参数审查机制（避免 --max-steps 默认值残留问题）。

### Baseline vs SFT 对比评估（D6）
- 在 50 条测试集上对比 baseline 和 SFT 模型。
- 修复 `eval_gsm8k.py` system prompt 不一致问题。
- 增加 `--adapter-path` 参数支持 LoRA 模型评估。
- 结果：pass@1 从 0.20 提升到 0.28，format_adherence 从 0.06 提升到 1.0。

---

## 最终指标

### 测试集对比（50 条 test_converted_500）

| 指标 | Baseline | SFT 后 | 变化 |
|------|----------|--------|------|
| pass_at_1 | 0.20 | 0.28 | **+8%** |
| answer_extraction_success | 0.98 | 1.0 | +2% |
| format_adherence | 0.06 | 1.0 | **+94%** |
| 提取方式 | numeric_fallback 为主 | 全部 boxed | 格式规范化 |

### 训练指标

| 指标 | 值 |
|------|-----|
| train_loss | 1.108 → 0.032 |
| 训练步数 | 375 steps |
| 训练数据 | 500 条 GSM8K |

### 说明

- Baseline 使用 `eval_gsm8k.py` 在 `test_converted_500.jsonl` 上评估
- SFT 使用相同测试集，通过 `--adapter-path` 加载 LoRA checkpoint
- system prompt 包含 `\boxed{}` 指令，与训练时一致
- 详细结果见 `runs/baseline_50_eval/` 和 `runs/sft_50_eval/`

---

## 关键问题与解决

### 1. Chat Template 双 Think Block 冲突
- **问题**：Qwen3 non-thinking prompt 自动插入空的 `<think></think>`，target 也从 `<think>` 开始，导致 full text 出现双 think block。
- **解决**：不能只看单条 target，必须检查最终送进模型的 full_text。
- **经验**：训练数据表面正确，不代表训练序列正确。

### 2. Loss 对齐错误（最关键）
- **问题**：旧脚本用 self-token prediction（`logits[:, :-1]` 对应 `labels[:, :-1]`），正确应该是 next-token prediction（`logits[:, :-1]` 对应 `labels[:, 1:]`）。
- **表现**：loss 表面下降，但生成分布被错误监督，训练后只输出换行、eval 全 0。
- **解决**：修正 label shift，确保 `logits[:, :-1]` 对齐 `labels[:, 1:]`。

### 3. Debug 能力不足
- **问题**：最初只能看 token id list，无法判断 prompt、target、full_text 和 loss mask 是否正确。
- **解决**：加入 `debug_batch.json`，输出 prompt_text、target、full_text、loss_mask_summary、shift_check。
- **原则**：SFT 训练脚本必须能 dump 文本级调试信息。

### 4. 并发训练冲突
- **问题**：多个 train_sft.py 并发运行导致内存和 run 结果混乱。
- **解决**：通过 `runs/.train_sft.lock` 做互斥。

### 5. 默认参数残留
- **问题**：`--max-steps` 默认值残留为 10，导致完整训练被覆盖。
- **原则**：训练脚本默认参数必须谨慎，会改变实验规模的参数不能 silent override。

### 6. Eval System Prompt 不一致
- **问题**：`eval_gsm8k.py` 使用 `"You are a helpful math assistant."`，但 SFT 训练时使用 `"You are a helpful math assistant. Solve the problem and put the final answer in \\boxed{}."`。导致 SFT 模型在 eval 时不输出 `\boxed{}` 格式，format_adherence 为 0%。
- **表现**：500 条测试集上 SFT 的 format_adherence=0%，pass@1 低于 baseline。
- **解决**：统一 system prompt，eval 脚本使用与训练一致的指令。
- **原则**：训练、推理、评估必须使用相同的 prompt 协议。

---

## 核心经验

1. **loss 下降不代表训练成功**：如果 label shift、loss mask 或 target 构造错了，loss 下降反而代表模型在学习错误目标。
2. **eval 出问题不一定模型坏了**：可能是答案抽取失败、格式判定错配、generation 截取错误、prompt 不一致。
3. **训练、推理、评估必须统一格式口径**：chat template、assistant header、target、eval_math 的 format checker 必须看同一个边界。
4. **数据选择要服务阶段目标**：先选低噪声任务（GSM8K），把训练链路跑稳。
5. **debug dump 是训练脚本必需品**：至少能看到 prompt、target、full_text、loss mask、label shift、generation diagnostics、checkpoint reload 结果。
6. **run 管理不是形式主义**：registry、metrics、hardware log、sample diff、checkpoint、run_report 能防止实验结果散落，出问题时提供排查路径。
7. **eval prompt 必须与训练 prompt 一致**：SFT 模型依赖 system prompt 中的格式指令，eval 时如果缺少 `\boxed{}` 指令，模型不会输出正确格式，导致 format_adherence 为 0%。

---

## M1 Validation

```bash
# 验证评估协议
python scripts/eval_math.py \
  --predictions data/math/toy/predictions.jsonl \
  --references data/math/toy/references.jsonl \
  --output data/math/toy/eval_results.jsonl \
  --summary data/math/toy/eval_summary.json

# 验证完整 SFT run
ls runs/000030_qwen3_0_6b_sft_gsm8k_500/
cat runs/000030_qwen3_0_6b_sft_gsm8k_500/run_meta.yaml

# 验证数据集
wc -l data/math/splits/sft_v1.jsonl

# 验证 baseline vs SFT 对比（50 条测试集）
cat runs/baseline_50_eval/eval_summary.json
cat runs/sft_50_eval/eval_summary.json
```

预期结果：
- `eval_math.py` 输出完整指标（pass_at_1、answer_extraction_success、format_adherence、avg_completion_length、invalid_output_rate）。
- SFT run 目录包含 config.yaml、run_meta.yaml（status=completed）、metrics/、samples/、checkpoints/、logs/。
- sft_v1.jsonl 有 500 条记录。
- baseline pass@1=0.20，SFT pass@1=0.28，format_adherence 从 0.06 提升到 1.0。

---

## M1 不做的事项

- 不做 DPO、GRPO、PPO
- 不做 Tool-call
- 不做多数据集混合
- 不做 4B/8B 训练
- 不追求 benchmark SOTA
- 不把失败 run 手工改成 completed

---

## M1 -> M2 过渡条件

M1 退出标准全部满足，run_report 明确建议进入 DPO。下一步进入 M2（0.6B Math DPO），需要：
1. 构造 chosen/rejected pairs
2. 验证 DPO 真实训练链路
3. 监控 preference margin、长度偏移、模式坍缩
