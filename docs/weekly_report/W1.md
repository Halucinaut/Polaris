Polaris M1 周复盘：踩坑记录与经验总结

这一周完成了 Polaris M1：在 Qwen3-0.6B 上跑通第一组 GSM8K Math SFT。M1 的结果本身比较顺利，但真正有价值的是中间暴露出来的一系列问题：数据格式、答案抽取、chat template、loss 对齐、eval 口径、run 管理、硬件边界。整体看下来，这一周的主要收获不是某个指标提升，而是对 SFT 最小闭环有了更具体的工程理解。

D1：从 toy data 开始固定评估协议

D1 的任务是阅读 Qwen3 的配置和 tokenizer 相关文件，并手写 toy data 来验证 eval_math.py。这个过程一开始比较痛苦，因为完全是从零开始，没有现成参考格式，也不确定什么样的输出应该算正确。

但手写一遍之后，数据格式和评估逻辑变得清晰了。我们确认了 Qwen3 里 <|im_start|>、<|im_end|> 属于对话边界，主要由 chat template 负责；<think>、</think>、\boxed{} 不是特殊 token，而是普通文本协议，需要通过 SFT 学出来。

D1 最大的收获是把三个指标拆开：pass_at_1、answer_extraction_success、format_adherence。这三个指标分别回答不同问题：答案是否正确，答案是否能被稳定抽取，输出是否符合我们定义的格式协议。后面排查问题时，这个拆分非常重要，因为它让我们能从结果反推原因，而不是把所有失败都混成“模型不行”。

D2：数据集选择和数据治理

D2 的主要任务是选择数据源，并构造小规模数据子集。我们比较了 GSM8K、OpenR1-Math-220K 和 MATH 三个数据集，对它们的字段结构、答案格式和适配风险有了更直观的认识。

这几个数据集的差异很明显。有的数据只有 question-answer，有的数据包含多条 reasoning trace；有的数据最终答案在 \boxed{} 里，有的数据用 #### 标记；有的数据难度较统一，有的数据跨度很大。最后选择 GSM8K，是因为 M1 的目标是跑通 SFT 闭环，而不是挑战数学能力上限。GSM8K 的答案稳定、题目难度适中、自动判分噪声低，更适合作为第一组 Math SFT 实验数据。

D2 的另一个收获是把数据源、license、字段结构、筛选规则、split 协议写进文档。manifest.yaml 和 screening_plan.md 不只是形式文档，它们的作用是让数据构造可复现。后面如果要换数据源、扩大训练规模或复查实验结果，不需要靠记忆回溯当时的选择。

D3：验证 MLX 环境、模型加载和 baseline eval

D3 相对顺利，主要验证了 MLX 环境、Qwen3-0.6B 模型加载和 baseline eval。我们确认了 mlx 和 mlx_lm 可以正常导入，也让 AI 辅助写了 load_model.py，验证本机可以加载 MLX 版 Qwen3-0.6B 并完成短文本生成。

这一天的关键不是 baseline 分数本身，而是确认真实模型可以接入前两天的数据和评估协议。我们在 D2 的 30 条 test 数据上跑了 baseline，并发现 Qwen3-0.6B 已经有一定 GSM8K 能力，但格式遵循并不稳定。这为 D5 的 SFT 对比提供了基线。

D3 还固定了一个重要工程原则：每个正式实验都必须通过 registry 注册，并记录规范的 metrics、hardware snapshot、sample output 和 run 状态。后面 D4/D5 的问题很多，但因为 run 结构被固定下来，排查才有依据。

D4：SFT sanity 是本周最难的一天

D4 是这周难度最高的一天。任务本身只是构造 SFT 数据并跑 10-step sanity，但实际暴露了多个典型训练系统问题。

第一个问题是 chat template 和 target 格式冲突。Qwen3 的 non-thinking prompt 会自动插入空的 <think></think>，而我们的 target 也从 <think> 开始，导致 full text 中出现了双 think block：

<|im_start|>assistant
<think>
</think>
<think>
...
</think>

这个问题说明，不能只看单条 target 是否正确，还必须看最终送进模型的 full_text。训练数据表面正确，不代表训练序列正确。

第二个问题是最关键的 loss 对齐错误。旧训练脚本存在 self-token prediction：用同一位置的 logits 去预测同一位置的 labels。但 causal LM 正确的训练目标应该是当前位置预测下一个 token，即 logits[:, :-1] 对齐 labels[:, 1:]。这个错误会导致 loss 表面下降，但生成分布被错误监督，最后出现训练后只输出换行、eval 全 0 的现象。

第三个问题是 debug 能力不足。最开始我们只能看到 token id list，无法判断 prompt、target、full_text 和 loss mask 是否正确。后来加入 debug_batch.json 后，才真正能定位问题。这个经验很重要：SFT 训练脚本必须能 dump 文本级调试信息，包括 prompt_text、target、full_text、loss_mask_summary 和 shift_check。只看 loss 曲线和 token id 不够。

D4 的另一个经验是，不能让 AI 直接“替你解决一切”。强模型可以很快修掉 bug，但如果中间没有诊断过程，自己很难获得训练系统的判断力。更好的方式是先让 AI 辅助生成代码和诊断信息，自己亲自看关键证据；实在定位不到问题时，再让更强的工具介入。Codex 最后修复得很快，但真正有价值的是修复前后我们看到了问题在哪里。

D5：完整 SFT 和实验闭环

D5 跑通了第一组完整 SFT。最终有效 run 是 runs/000030_qwen3_0_6b_sft_gsm8k_500，状态为 completed。训练集 500 条，训练 375 steps，loss 从 1.108333 降到 0.032215，没有 NaN 或 Inf。评估结果也比较清楚：pass_at_1=0.8，answer_extraction_success=1.0，format_adherence=0.9，invalid_output_rate=0.0。

D5 也暴露了两个工程风险。第一个是多个 train_sft.py 并发运行导致内存和 run 结果混乱，后面通过 runs/.train_sft.lock 做了互斥。第二个是 --max-steps 默认值残留为 10，导致不传参数时完整训练仍被覆盖成 10 步。这个问题说明训练脚本的默认参数必须非常谨慎，尤其是会改变实验规模的参数，不能 silent override。

硬件方面，MBP 可以完成这类小规模本地训练，但也能明显感受到边界：训练时发热很快，GPU 占用率会直接拉满，内存占用反而不是最明显瓶颈。这说明 MacBook 适合做 idea 验证、小模型闭环和调试，不适合无脑堆大规模训练。

本周核心经验

这周最大的收获是对 SFT 阶段的数据结构、答案抽取、数据集选择、训练脚本和问题排查有了更具体的理解。

第一，loss 下降不代表训练成功。loss 只说明某个目标函数被优化了，但如果 label shift、loss mask 或 target 构造错了，loss 下降反而可能代表模型正在学习错误目标。

第二，eval 出问题也不一定说明模型坏了。eval 全 0 可能来自答案抽取失败、格式判定规则错配、generation 截取错误、prompt 不一致，也可能来自训练后生成异常。必须先拆指标，再查链路。

第三，训练、推理、评估必须统一格式口径。chat template、assistant header、target、eval_math 的 format checker 必须看的是同一个边界。如果训练让模型生成完整 <think>...</think>\n\n\boxed{}，eval 也必须按这个完整 continuation 来判断。

第四，数据选择要服务阶段目标。M1 选择 GSM8K 是因为它让失败更容易归因。MATH 和 OpenR1-Math 更复杂，但不适合第一组闭环实验。第一阶段应该先选择低噪声任务，把训练链路跑稳。

第五，debug dump 是训练脚本的必需品。以后任何 SFT/RL 脚本都应该内置可审计输出，至少能看到 prompt、target、full_text、loss mask、label shift、generation diagnostics 和 checkpoint reload 结果。

第六，run 管理不是形式主义。registry、metrics、hardware log、sample diff、checkpoint 和 run_report 能防止实验结果散落，也能在出问题时提供排查路径。

最终结论

M1 的价值不是这次 500 条 GSM8K SFT 指标本身，而是完整跑通了一个本地 post-training 最小闭环：数据构造、格式协议、baseline eval、SFT 数据生成、10-step sanity、完整 SFT、sample diff 审阅、run_report 记录。

这周之后，我对 SFT 的理解从“调用训练脚本”推进到了“能判断训练目标是否正确、评估口径是否一致、指标变化是否可信”。后面进入 DPO/GRPO 时，仍然要沿用这套思路：先固定数据和评估协议，再跑小规模 sanity，先查数据和 mask，再看 loss 和 reward，最后才讨论模型能力。